{
  "name": "mahal-v1",
  "private": true,
  "description": "Root-level build orchestration for single-service deploys (e.g. Render). Local dev still runs frontend and backend separately — see README.",
  "scripts": {
    "build": "npm --prefix frontend ci && npm --prefix frontend run build && npm --prefix backend ci",
    "start": "npm --prefix backend start"
  }
}
